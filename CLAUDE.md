# 환경분석맵 (Environment Analysis Map)

이승원 대표(Andy)의 프로젝트다. 존댓말. 팩트 기반, 추정 금지(불확실은 '확인 필요'). 환각 금지. 리스크는 직설적으로 지적한다.

> ⚠ 저장소 이름 `gijang-map`(→ `hayday-envmap`)·클라우드타입 서비스명 `gijang-map` 은
> **기장 하나 만들 때 지은 옛 이름**이다. **바꾸지 않는다**(배포가 끊긴다).
> 문서·대화에서는 언제나 **환경분석맵**이라 부른다.
> 「지도삽도엔진」「지도툴」「도시환경 분석지도」는 전부 같은 것이고 **전부 옛 이름이다.**

## 세션 시작 시 먼저 읽을 것

본부(HQ) = `/Users/iseung-won/Library/CloudStorage/GoogleDrive-ganpan0@gmail.com/내 드라이브/hayday-hq`

**어느 도구(클로드 코드·챗지피티·헤르메스·코워크)에서 시작하든 순서는 같다.**

| 순서 | 파일 | 내용 |
|---|---|---|
| 1 | `03_시스템_도구/환경분석맵/00_폴더규칙.md` | 제품 구성·폴더 규칙·용어. **여기부터** |
| 2 | `03_시스템_도구/환경분석맵/04_미결.md` | 지금 막힌 것 |
| 3 | `03_시스템_도구/환경분석맵/03_진행기록.md` | 최근 경과 |
| 4 | `03_시스템_도구/환경분석맵/대상지_<지역>/00_개요.md` | 그 지역만의 규칙 |
| 5 | `.claude/환경분석맵_SOP.md` | 절차·명령어 (헤르메스 스킬 `map-illustration-engine-sop` 과 같은 내용) |

⚠ `03_시스템_도구/지도삽도엔진/` 은 **죽은 폴더다.** 2026-08-13에 합쳤다. 거기서 작업하지 않는다.

## 화면 8종 (2026-09-02)

| 종류 | 화면 | 파일 |
|---|---|---|
| — | 첫 화면 | `index.html` |
| 대상지 | 기장 만화천 | `gijang.html` |
| 대상지 | 부산 서구 전역 | `seogu.html` |
| 대상지 | 부산 사하 신평·장림·다대동 | `saha.html` |
| 대상지 | 포항 원도심 3권역 | `pohang.html` |
| 대상지 | 김해 원도심 | `gimhae-wondosim.html` |
| 주제도 | 부산 도로교통망 | `busan-traffic.html` |
| 도구 | GIS | `gis.html` |

라이브 — https://web-gijang-map-mrksc1tcf2e7efab.sel3.cloudtype.app/

## 🔴 고칠 때 지킬 3줄 — 어긴 적 있다

2026-08-18, 다른 방이 지도 7개를 한꺼번에 올려 김해 작업을 통째로 덮어썼다.

1. **라이브 파일을 먼저 받아 그 위에 고친다.** 내 사본을 그대로 올리지 않는다
2. **내가 고친 파일만 올린다.** 일괄 업로드 금지
3. 올리기 전 **원격이 앞서 있는지 확인**한다 (`git fetch origin && git status`)

## 배포

```bash
cd ~/dev/gijang-map && git push origin main
```
- 인증 = 전용 키 `~/.ssh/id_ed25519_hayday` (저장소 `core.sshCommand` 고정).
  기존 `~/.ssh/id_ed25519` 는 **다른 계정** 것이다. 섞지 말 것
- ⚠ **클라우드타입은 자동 배포가 없다.** 콘솔에서 케밥(⋮) → **재배포** 를 눌러야 반영된다
- 검증은 `curl -sS --compressed <URL>/<파일> | md5 -q` 로 로컬과 대조

## 빌드 파이프라인 — 서구만 있다

```bash
python3 build/gen.py seogu --check   # 대조만
python3 build/gen.py seogu           # 재생성
python3 build/extract.py seogu       # 배포본 HTML → 데이터+템플릿 (역방향)
```
- 검증 기준은 **재생성물이 배포본과 바이트 단위로 일치** 하나뿐이다
- 🔴 **HTML을 직접 고쳤으면 반드시 `extract.py` 를 돌려 되먹인다.** 안 하면 다음 빌드에서 소멸한다
- `index.html` 은 출력 대상에서 하드 차단됨
- **나머지 7개 화면은 파이프라인이 없다. 직접 편집이 유일하다**

## 규칙

- 인물 호칭은 **남실장님** 고정. 이름 기반 표기 금지
- API 키·토큰 값은 코드·문서 어디에도 적지 않는다. 실값은 노션 API 키 트래커에만 있다
- 대용량 HTML(5~8MB) 편집 시 앵커는 `repr()` 로 바이트 확인 후 assert 하고 치환한다
- MapLibre `symbol` 레이어 사용 불가(glyphs 없음) → 라벨은 `maplibregl.Marker` DOM
- 초기화 재시도는 **시간 기준**으로 한다(횟수 기준 금지 — 느린 회선에서 한도가 소진돼 앱이 안 뜬다)
- 작업이 끝나면 `03_시스템_도구/환경분석맵/03_진행기록.md`·`04_미결.md` 에 남긴다
